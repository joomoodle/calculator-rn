import {Text} from 'react-native';

const Title = () => {
  return (
    <Text
      style={{
        color: '#0866ff',
        fontSize: 28,
        fontWeight: '700',
      }}>
      Cara libro
    </Text>
  );
};

export default Title;
